# RL-Template から始めるコンペの進め方

このリポジトリは、Rust を主言語とする強化学習 Kaggle コンペのテンプレート。  
下のフェーズの順に実装と改善を進めていく。

## フェーズ 0: このテンプレートが合うかを判断する

このフェーズの目的は、参加するコンペにこのテンプレートが合うかを決めることになる。合わなければ、ここで止まる。

- [ ] ユーザーにコンペの URL を貼ってもらう。貼られるまで先に進まない
- [ ] URL からコンペのルール（概要、ゲームの内容、評価方式、提出形式、締切）を読み、`docs/competition/rules.md` に書く
- [ ] 投票数の上位10件の公開 notebook を読み、それぞれの要点を `docs/competition/notebooks.md` に書く
- [ ] 投票数の上位10件の discussion を読み、それぞれの要点を `docs/competition/discussions.md` に書く
- [ ] 3つのファイルを根拠に、このテンプレートが合うかを判断してユーザーに伝える

合う条件は次の3つになる。

| 条件 | 合わない例 |
| --- | --- |
| ボット同士を対戦させるシミュレーション形式である | 表形式データの予測、画像分類 |
| 自分のボットを提出して、環境の中で動かす | 予測結果の CSV を提出する |
| 行動が離散で、探索と自己対戦が成り立つ | 連続制御だけで探索の余地が無い |

完了条件は、`docs/competition/` の3ファイルが揃い、合うかどうかの判断をユーザーに伝えたことになる。

## フェーズ 1: 対戦環境を Rust で作る

このフェーズが最も重要になる。高速に対戦を回せる環境が無いと、検証も学習も始まらない。`docs/competition/notebooks.md` と `discussions.md` の深読みは後回しにして、まず環境を作る。

- [ ] `docs/competition/rules.md` からゲームの形式を決める（2人零和の手番制か、N人の同時手番か）
- [ ] `core/env` の trait を決めて、ルール（状態遷移、合法手、終局判定）を実装する
- [ ] `core/arena` の対戦ループを実装する
- [ ] `crates/arena` の `match` サブコマンドを実装し、ランダム方策同士で対戦できるようにする
- [ ] Kaggle から上位プレイヤーの episode の replay を取得する
- [ ] 公式の環境（kaggle-environments）と一致することを、下の4条件で確かめる
- [ ] 1局あたりの時間を測り、`docs/changelog/improvements/` に記録する

完了条件は、ランダム同士の対戦が並列で回ること（`arena match` で1000局を回して、局数と所要時間が出る）と、次の4条件が全て満たされることになる。「数十局で一致した」は「不一致を見つけられなかった」でしかないので、条件にしない。

| 条件 | 確認の方法 |
| --- | --- |
| 公式の対局記録を再生して一致する | 上位プレイヤーの episode の replay を 1000 局以上 Rust の env に流し、全ステップで状態と合法手の集合が一致する。実戦の分布を踏むので、ランダムでは出ない局面を覆う |
| 生成した対局でも一致する | ランダムと、偏りを付けた方策（攻め寄り、長期化寄り）で数万局を両環境に流し、全ステップで一致する |
| ルール文の項目ごとに検証がある | `rules.md` のルールを1行ずつ列挙し、各行に対応する一致の検証を最低1つ持つ。対応表を `docs/competition/rules.md` に残す |
| ルール文に無い挙動が一致する | 不正手を出したとき、時間切れのとき、最大ターンに達したときの結果が公式と同じになる |

## フェーズ 2: ladder を作る

フェーズ 1 で作った対戦環境を、身内が使えて、ブラウザで見られる形にする。学習より先に作るのは、対戦環境ができた時点で人手の方策や簡単なヒューリスティックを持ち寄って比べられるようにするためになる。画面と機能の要件は `ladder/README.md` にある。作業の前に読む。

- [ ] `crates/arena match` の引数と stdout の JSON を固める。これが `ladder/server` との契約になる
- [ ] `ladder/server` の DB スキーマ（submissions、matches、results）を決める
- [ ] `ladder/server` の API（重みの提出、対戦の一覧、順位表）と `runner`（`arena` をサブプロセスで実行）を実装する
- [ ] 組み合わせ（誰と誰を対戦させるか）と順位付け（勝率か、レーティングか）を決めて実装する
- [ ] `ladder/web` で順位表と対戦履歴の画面を作る。API の型は kubb で生成する
- [ ] `ladder/Dockerfile` の多段ビルドを書き、1コマンドで立ち上がるようにする
- [ ] 動作確認は組み込みの方策（ランダム、ヒューリスティック）で行う。この時点では学習済みの重みが無いので、`ladder` は組み込みの方策も登録できるようにする

完了条件は、次の3つになる。

| 条件 | 確認の方法 |
| --- | --- |
| 1コマンドで立ち上がる | Docker のビルドと起動で、ブラウザから順位表が開く |
| 提出から順位表まで通る | 方策を2つ提出すると、対戦がキューに入り、`runner` が回し、結果が順位表に出る |
| 身内が使える | 自分以外の1人が、手順書だけを見て提出できる |

## フェーズ 3: ルール方策を作る

学習の前に、if と else だけで書いた静的な方策を作る。目的は2つある。評価の基準になるモデルを持つことと、模倣学習の教師を持つことになる。

- [ ] `docs/competition/` を読んで、基本の動作（合法手の中から明らかに良い手を選ぶ）と、多少の応用（数手先の単純な読み、定石）を列挙する
- [ ] `core/agent` にルール方策として実装する。`Policy` trait を満たす形にする
- [ ] `crates/arena` の組み込み方策として名前で指定できるようにする
- [ ] ladder に登録して、ランダム方策との対戦で強さを確かめる
- [ ] 版を付ける。名前は `stat_v<自然数>` にする（`stat_v1`、`stat_v2`、…）。後の学習モデルの比較基準になるので、書き換えずに版を増やす

ここから先は、**ユーザーが「停止」と言うまで**改善を繰り返す。どこまで強くすれば十分かは決められないので、終わりの判断はユーザーが持つ。1周は次の順になる。

```text
改善案を1つ選ぶ
  → 新しい版として実装する（前の版は残す）
  → ladder で前の版と対戦させる
  → 勝率と標準偏差を見て、採用するか決める
  → 採用したら docs/changelog/improvements/ に、採用しなかったら docs/changelog/downgrades/ に書く
  → ユーザーに結果を報告して、次の案に進む
```

- [ ] 改善案は1周に1つにする。複数を同時に入れると、どれが効いたか分からなくなる
- [ ] 採用した案は `docs/changelog/improvements/` に書く。何を変えたか、前の版との勝率、なぜ効いたと考えるかを残す
- [ ] 採用しなかった案も `docs/changelog/downgrades/` に**同じ粒度で**書く。同じ案を後で試し直さないための記録になる
- [ ] 毎周の終わりにユーザーへ報告する。停止の指示が出るまで、次の周を続ける

完了条件は、ユーザーが停止と言ったことになる。停止した時点の最新の版が、以後の評価の基準になる。

**このフェーズの完了を待たずに、フェーズ 4 を並列で始める。** `stat_v1` が ladder に載った時点で、フェーズ 4 に着手してよい。フェーズ 3 の改善はその後も続ける。

## フェーズ 4: 学習の基盤を作る

フェーズ 3 と並列に進める。着手の条件は `stat_v1` が ladder に載っていることになる。触るファイルはフェーズ 3 とほぼ重ならない（フェーズ 3 は `core/agent` のルール方策、フェーズ 4 は `core/encode`、`core/search`、`infer`、`crates/arena` の `selfplay`、`rl_template`）。`Policy` trait だけは両方が使うので、変えるときは両方に知らせる。

依存の順に埋める。上のものが動かないと、下のものを試せない。学習は模倣から始める。ルール方策のログを真似るところまでを先に通し、その上に PPO や AlphaZero を積む。教師はそのときの最新の `stat_v<n>` を使い、フェーズ 3 で版が進んだら模倣をやり直す。

| 順 | 対象 | 決めること | 動作の確認 |
| --- | --- | --- | --- |
| 1 | `core/encode` | テンソルの形 | 1局面をテンソルにして形が合う |
| 2 | `core/search` | MCTS か alpha-beta か | 組み込みの評価で1手読める |
| 3 | `infer` | ONNX の入出力の形 | ONNX を読んでバッチ推論が返る |
| 4 | `crates/arena` の `selfplay` | `samples` の形式 | ルール方策同士の対局を `samples` に書き出せる |
| 5 | `rl_template` の模倣 | 行動の出し方（分類かランキングか） | ルール方策のログから学習した ONNX が、`arena match` でルール方策と同等の勝率になる。この ONNX が `rl_v1` になる |
| 6 | `rl_template` の強化 | PPO か AlphaZero か | `rl_v1` を初期値にして、1イテレーション回る |
| 7 | `crates/submission` | 提出形式に合わせた届け方 | 提出形式でビルドできる |

`crates/py` は解析で必要になったときに埋める。順序には入れない。

完了条件は、模倣した ONNX が `arena match` でルール方策と同等の勝率になり、ladder の順位表に載っていることになる。

## フェーズ 5: 学習と評価

1イテレーションは次の順になる。

```text
arena selfplay --weights rl_v(n).onnx
  → rl_template で学習 → rl_v(n+1).onnx を書き出す
  → arena match rl_v(n) vs rl_v(n+1)
  → 勝率を見て採用を決める
  → docs/changelog/ に記録する
```

- [ ] 学習モデルの名前は `rl_v<自然数>` にする（`rl_v1`、`rl_v2`、…）。古い版のファイルを書き換えない
- [ ] ladder には `rl_v<n>` と `stat_v<n>` の両方を載せて、静的モデルとの差も見る
- [ ] 採用した変更は `docs/changelog/improvements/` に書く
- [ ] 採用しなかった変更も `docs/changelog/downgrades/` に**同じ粒度で**書く
- [ ] 重い処理は Mac で回さず GPU 機で回す
- [ ] Python に1ステップごとのループを書かない。観測のエンコードを Python で再実装しない
- [ ] 手詰まりになったら `docs/competition/notebooks.md` と `discussions.md` に戻り、深く読むものを `docs/research/` に書く

完了条件は、各イテレーションに changelog の記録が1つあることになる。

## 提出

提出はフェーズに入れない。ユーザーが指定したタイミングで行う。指示が出たら、`crates/submission` を提出形式でビルドし、サイズの上限と Kaggle の環境で1局動くことを確かめてから提出する。
