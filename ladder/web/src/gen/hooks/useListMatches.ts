/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { ListMatchesOptions, ListMatchesStatus200 } from '../types/ListMatches'
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query'
import { listMatches } from '../clients/listMatches'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const listMatchesQueryKey = ({ query }: Omit<ListMatchesOptions, 'headers'> = {}) => [{ url: '/matches' }, ...(query ? [query] : [])] as const

type ListMatchesQueryKey = ReturnType<typeof listMatchesQueryKey>

export function listMatchesQueryOptions({ query }: ListMatchesOptions = {}, config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = listMatchesQueryKey({ query })
  return queryOptions<ListMatchesStatus200, ResponseErrorConfig<Error>, ListMatchesStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      return listMatches({ ...config, query, signal: config.signal ?? signal, throwOnError: true }).unwrap()
   },
  })
}

/**
 * {@link /matches}
 */
export function useListMatches<TData = ListMatchesStatus200, TQueryData = ListMatchesStatus200, TQueryKey extends QueryKey = ListMatchesQueryKey>({ query }: { query?: ListMatchesOptions['query'] | (() => ListMatchesOptions['query']) } = {}, options: {
  query?: Partial<QueryObserverOptions<ListMatchesStatus200, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>>
} = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const resolvedParams = { query: typeof query === 'function' ? query() : query }
  const queryKey = resolvedOptions?.queryKey ?? listMatchesQueryKey(resolvedParams)

  const queryResult = useQuery({
   ...listMatchesQueryOptions(resolvedParams, config),
   ...resolvedOptions,
   queryKey,
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  queryResult.queryKey = queryKey as TQueryKey

  return queryResult
}
