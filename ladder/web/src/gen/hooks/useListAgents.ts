/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { RequestConfig, ResponseErrorConfig } from '../.kubb/client'
import type { ListAgentsStatus200 } from '../types/ListAgents'
import type { QueryKey, QueryClient, QueryObserverOptions, UseQueryResult } from '@tanstack/react-query'
import { listAgents } from '../clients/listAgents'
import { queryOptions, useQuery } from '@tanstack/react-query'

export const listAgentsQueryKey = () => [{ url: '/agents' }] as const

type ListAgentsQueryKey = ReturnType<typeof listAgentsQueryKey>

export function listAgentsQueryOptions(config: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>> = {}) {
  const queryKey = listAgentsQueryKey()
  return queryOptions<ListAgentsStatus200, ResponseErrorConfig<Error>, ListAgentsStatus200, typeof queryKey>({
   queryKey,
   queryFn: async ({ signal }) => {
      return listAgents({ ...config, signal: config.signal ?? signal, throwOnError: true }).unwrap()
   },
  })
}

/**
 * {@link /agents}
 */
export function useListAgents<TData = ListAgentsStatus200, TQueryData = ListAgentsStatus200, TQueryKey extends QueryKey = ListAgentsQueryKey>(options: {
  query?: Partial<QueryObserverOptions<ListAgentsStatus200, ResponseErrorConfig<Error>, TData, TQueryData, TQueryKey>> & { client?: QueryClient },
  client?: Partial<Omit<RequestConfig, 'path' | 'query' | 'body' | 'headers' | 'url'>>
} = {}) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? listAgentsQueryKey()

  const queryResult = useQuery({
   ...listAgentsQueryOptions(config),
   ...resolvedOptions,
   queryKey,
  } as unknown as QueryObserverOptions, queryClient) as UseQueryResult<TData, ResponseErrorConfig<Error>> & { queryKey: TQueryKey }

  queryResult.queryKey = queryKey as TQueryKey

  return queryResult
}
